# Copy to .env and adjust. The path is relative to prisma/schema.prisma,
# so this creates prisma/db/custom.db (ignored by git).
DATABASE_URL="file:./db/custom.db"

# Set a long random value in any real deployment.
JWT_SECRET="change-me"
